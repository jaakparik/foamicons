{
  "name": "@foamicons/figma-plugin",
  "version": "0.9.16",
  "description": "Figma plugin for Foamicons",
  "scripts": {
    "generate": "tsx scripts/generate-icons.ts",
    "build": "pnpm run generate && tsx scripts/build.ts",
    "dev": "pnpm run generate && tsx scripts/build.ts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@foamicons/core": "workspace:*"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
